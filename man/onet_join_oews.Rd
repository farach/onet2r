% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oews.R
\name{onet_join_oews}
\alias{onet_join_oews}
\title{Join O*NET Occupations to National OEWS Estimates}
\usage{
onet_join_oews(
  occupations,
  oews = NULL,
  year = latest_oews_year(),
  by = "code",
  cache_dir = tools::R_user_dir("onet2r", "cache"),
  force = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{occupations}{A data frame containing an O*NET occupation code column.}

\item{oews}{Optional OEWS tibble. If omitted, \code{onet_oews_national()} is called.}

\item{year}{Integer OEWS estimate year, used when \code{oews} is omitted.}

\item{by}{Name of the occupation code column in \code{occupations}.}

\item{cache_dir}{Directory used to cache the downloaded BLS ZIP file.}

\item{force}{Logical; if \code{TRUE}, re-download OEWS data when \code{oews} is omitted.}

\item{quiet}{Logical; if \code{FALSE}, show download progress.}
}
\value{
A tibble containing \code{occupations} plus a \code{soc_code} column and
matching national OEWS employment and wage estimate columns.
}
\description{
Joins O*NET occupation data to national OEWS employment and wage estimates by
converting detailed O*NET-SOC codes such as \code{"15-1252.00"} to SOC codes such
as \code{"15-1252"}.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}
}
\section{Lifecycle}{

This helper is soft-deprecated in favor of \code{\link[=onet_weight_panel_oews]{onet_weight_panel_oews()}} plus
\code{\link[=onet_measure_aggregate]{onet_measure_aggregate()}} for vintage-aware weighting.
}

\examples{
occupations <- tibble::tibble(
  code = c("15-1252.00", "29-1141.00"),
  title = c("Software Developers", "Registered Nurses")
)

oews <- tibble::tibble(
  occ_code = c("15-1252", "29-1141"),
  occ_title = c("Software Developers", "Registered Nurses"),
  tot_emp = c(1847900, 3175400),
  a_median = c(133080, 93070)
)

suppressWarnings(onet_join_oews(occupations, oews = oews))
}
