% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weighted.R
\name{onet_weighted_summary}
\alias{onet_weighted_summary}
\title{Summarise O*NET Values with Employment or Wage Weights}
\usage{
onet_weighted_summary(
  data,
  group,
  value,
  occupation_code = "code",
  oews = NULL,
  weight = "tot_emp",
  wage = "a_median"
)
}
\arguments{
\item{data}{A data frame containing one row per occupation-value pair.}

\item{group}{Character vector of columns to group by, such as task or skill
identifiers.}

\item{value}{Name of the numeric O*NET value column to average.}

\item{occupation_code}{Name of the occupation code column in \code{data}.}

\item{oews}{Optional OEWS tibble. If supplied and \code{weight} is not already in
\code{data}, it is joined with \code{\link[=onet_join_oews]{onet_join_oews()}}.}

\item{weight}{Name of the employment weight column. Defaults to \code{tot_emp},
the OEWS total-employment column.}

\item{wage}{Name of the wage column used for wage-weighted summaries.}
}
\value{
A tibble with \code{group} columns plus \code{n_records}, \code{n_occupations},
\code{total_weight}, \code{weighted_mean}, and \code{wage_weighted_mean}.
}
\description{
Computes employment-weighted summaries for O*NET task, skill, ability, or
work-activity rows after joining occupation-level employment and wage
estimates. This is useful for moving from occupation-level O*NET outputs to
labor-market-weighted measures.
}
\section{Lifecycle}{

This helper is soft-deprecated for measure work. Prefer
\code{\link[=onet_weight_panel_oews]{onet_weight_panel_oews()}}, \code{\link[=onet_weight_panel_pums]{onet_weight_panel_pums()}}, and
\code{\link[=onet_measure_aggregate]{onet_measure_aggregate()}} for vintage-aware weighting.
}

\examples{
skills <- tibble::tibble(
  code = c("15-1252.00", "15-1252.00", "29-1141.00", "29-1141.00"),
  element_id = c("2.A.1.a", "2.A.1.b", "2.A.1.a", "2.A.1.b"),
  element_name = c(
    "Reading Comprehension", "Active Listening",
    "Reading Comprehension", "Active Listening"
  ),
  data_value = c(4.12, 4.00, 3.88, 4.25)
)

oews <- tibble::tibble(
  occ_code = c("15-1252", "29-1141"),
  tot_emp = c(1847900, 3175400),
  a_median = c(133080, 93070)
)

suppressWarnings(
  onet_weighted_summary(
    skills,
    group = c("element_id", "element_name"),
    value = "data_value",
    oews = oews
  )
)
}
