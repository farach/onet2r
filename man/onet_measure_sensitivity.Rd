% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{onet_measure_sensitivity}
\alias{onet_measure_sensitivity}
\title{Stress Test a User-Supplied Measure}
\usage{
onet_measure_sensitivity(
  measure,
  weight_panels,
  bridges = list(no_bridge = NULL),
  task_ratings = NULL,
  task_metadata = NULL,
  include_supplemental = FALSE,
  weight_scale = "RT",
  year = NULL,
  cell = NULL,
  baseline = NULL
)
}
\arguments{
\item{measure}{An \code{onet_measure} object.}

\item{weight_panels}{A weight-panel data frame or named list of weight-panel
data frames.}

\item{bridges}{Optional bridge data frame, \code{NULL}, or named list of bridges.}

\item{task_ratings}{For task-level measures, a task-ratings data frame or
named list of task-ratings data frames. Release provenance uses
\code{release_version}, then row-wise \code{version} where \code{release_version} is
missing. When neither column exists, an explicit list name is used.}

\item{task_metadata}{Optional task metadata data frame or named list matching
\code{task_ratings}.}

\item{include_supplemental}{Logical vector. For task-level measures, controls
whether Supplemental tasks are included.}

\item{weight_scale}{Character vector of task rating scale ids. Defaults to
\code{"RT"}, the Task Ratings scale for Relevance of Task.}

\item{year}{Optional single year passed to \code{\link[=onet_measure_aggregate]{onet_measure_aggregate()}}.}

\item{cell}{Optional cell filter passed to \code{\link[=onet_measure_aggregate]{onet_measure_aggregate()}}.}

\item{baseline}{Optional scenario label used as the movement baseline.}
}
\value{
A tibble with one row per scenario, aggregate results, movement
fields, and provenance list-column metadata. The movement fields compare
scenario aggregates with the selected baseline; they are not rank,
quintile, variance, or content-drift diagnostics.
}
\description{
Runs the same user-supplied measure through alternative weight panels,
bridges, and task-handling choices. The package does not create the
substantive measure. It only changes the plumbing around that measure.
}
\details{
\code{weight_panels} must contain employment weight panels with
\code{reference_soc_code}, \code{year}, \code{employment}, \code{weight_share}, and provenance
columns. Output from \code{\link[=onet_content_change]{onet_content_change()}} describes task-content movement
and is not a weight panel. Pass named lists of single-release \code{task_ratings}
and matching \code{task_metadata} to compare task-handling scenarios across
releases.
}
\examples{
scores <- tibble::tibble(
  onet_soc_code = c("15-1252.00", "29-1141.00"),
  score = c(0.7, 0.2)
)
measure <- onet_measure(scores, "onet_soc_code", "score")
weights <- tibble::tibble(
  reference_soc_code = c("15-1252", "29-1141"),
  year = 2024L,
  employment = c(100, 300),
  weight_share = c(0.25, 0.75),
  source = "fixture",
  source_taxonomy = "2018 SOC",
  reference_taxonomy = "2018 SOC"
)
onet_measure_sensitivity(measure, weights)
}
