% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/census.R
\name{onet_pums_employment_weights}
\alias{onet_pums_employment_weights}
\title{Create SOC Employment Weights from PUMS Microdata}
\usage{
onet_pums_employment_weights(pums, socp = "SOCP", weight = "PWGTP")
}
\arguments{
\item{pums}{A data frame containing individual-level occupation records.}

\item{socp}{Name of the occupation-code column. ACS PUMS commonly uses
\code{"SOCP"}.}

\item{weight}{Optional name of the person-weight column. If \code{NULL}, each row
receives weight 1. ACS PUMS commonly uses \code{"PWGTP"}.}
}
\value{
A tibble with \code{soc_code}, \code{employment}, and \code{records}.
}
\description{
Converts American Community Survey (ACS), Current Population Survey (CPS), or
similar microdata with occupation codes into SOC-level employment weights.
Use this after downloading PUMS data with packages such as \code{tidycensus} or
\code{ipumsr}.
}
\examples{
pums <- tibble::tibble(
  SOCP = c("151252", "151252", "291141"),
  PWGTP = c(120, 80, 200)
)

onet_pums_employment_weights(pums)
}
