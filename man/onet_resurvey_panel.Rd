% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resurvey.R
\name{onet_resurvey_panel}
\alias{onet_resurvey_panel}
\title{Assemble a Task by Resurvey-Cycle Panel}
\usage{
onet_resurvey_panel(
  panel,
  scale = "IM",
  item = "task_id",
  survey_sources = c("Incumbent", "Occupational Expert"),
  min_importance = NULL
)
}
\arguments{
\item{panel}{A tibble from \code{\link[=onet_panel]{onet_panel()}} (a Task Ratings panel) or the same
schema. Must contain \code{onet_soc_code}, \code{release_version}, \code{release_date},
\code{soc_vintage}, \code{source_date}, \code{domain_source}, \code{data_value}, and the \code{item}
key column. \code{scale_id} is required when \code{scale} is not \code{NULL}.}

\item{scale}{Optional scale id to filter to a single rating, for example
\code{"IM"} for Importance. Filtering keeps one row per occupation, item, and
release. Use \code{NULL} to keep every scale.}

\item{item}{Column that identifies the content item within an occupation.
Defaults to \code{"task_id"}.}

\item{survey_sources}{Character vector of \code{domain_source} values that count
as an incumbent survey rotation. Defaults to Incumbent and Occupational
Expert. Matching is case-insensitive and by substring.}

\item{min_importance}{Optional numeric floor applied to \code{data_value} (on the
selected \code{scale}). Rows below the floor, or with missing \code{data_value}, are
dropped. Use it to reproduce the Importance filter that removes the
post-v21.0 Task Relevance artifact.}
}
\value{
A tibble with one row per occupation, item, and release. Alongside
the identifier and rating columns it carries the resurvey clock:
\code{survey_source} (survey, analyst, other, or unknown), \code{occ_survey_date}
(the occupation survey clock, the latest survey \code{source_date} in that
release), \code{prev_survey_date}, \code{prev_release_version}, \code{resurvey_event}
(the survey clock advanced versus the occupation's prior release within the
same taxonomy era), \code{cycle_index} (cumulative resurvey count),
\code{age_resolved} (years of staleness a resurvey resolved), and the incoming
seam flags \code{seam_in} and \code{seam_type}.
}
\description{
Restructures a longitudinal O*NET panel into the task by resurvey-cycle
frame used to study when occupations are actually re-rated. O*NET publishes
an occupation survey \code{source_date} on each Task Ratings row and rotates its
incumbent-worker survey across only part of the taxonomy each year, so a cell
is an observation of change only when the occupation was re-rated between two
releases. This function exposes that resurvey clock at the task level.
}
\details{
The occupation survey clock is the newest \code{source_date} among rows whose
\code{domain_source} is a survey source; Analyst - Transition carry-forward rows
are excluded from the clock. Resurvey events and cycle indices are computed
within each \code{soc_vintage} era so the v25.1 SOC seam never registers as a
resurvey. Pair this frame with \code{\link[=onet_condition_on_resurvey]{onet_condition_on_resurvey()}} to obtain the
at-risk set and its selection reasons.
}
\examples{
panel <- tibble::tibble(
  release_version = rep(c("22.1", "23.1"), each = 2),
  release_date = rep(as.Date(c("2017-10-01", "2018-11-01")), each = 2),
  soc_vintage = "2010",
  onet_soc_code = rep(c("15-1132.00", "29-1141.00"), 2),
  soc_code = rep(c("15-1132", "29-1141"), 2),
  task_id = rep(c("1001", "1002"), 2),
  task = rep(c("Write code.", "Assess patients."), 2),
  scale_id = "IM",
  data_value = c(4.1, 4.6, 4.1, 4.8),
  source_date = as.Date(c("2016-07-01", "2016-07-01", "2016-07-01", "2018-07-01")),
  domain_source = "Incumbent"
)
onet_resurvey_panel(panel)
}
