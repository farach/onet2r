% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{onet_cache_use}
\alias{onet_cache_use}
\title{Configure O*NET API Response Caching}
\usage{
onet_cache_use(
  enabled = TRUE,
  cache_dir = tools::R_user_dir("onet2r", "cache")
)
}
\arguments{
\item{enabled}{Logical; enable or disable response caching.}

\item{cache_dir}{Directory where cached API responses should be stored.}
}
\value{
Invisibly returns a list with the active cache settings.
}
\description{
Enables or disables local caching for O*NET API responses. Caching is off by
default because API results may change over time, but it is useful when
developing analyses that repeatedly request the same endpoints.
}
\details{
Cached Web Services responses are reused until the caller disables caching
or clears them with \code{\link[=onet_cache_clear]{onet_cache_clear()}}. The package does not attach a
time-to-live, so enable caching only when stale O*NET API responses are
acceptable for the analysis. Cache clearing waits for active writes and
verified source-receipt transactions to finish before removing a section.
}
\examples{
onet_cache_use(enabled = FALSE)
}
