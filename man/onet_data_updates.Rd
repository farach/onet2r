% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_updates.R
\name{onet_data_updates}
\alias{onet_data_updates}
\title{Get the O*NET Longitudinal Data Updates Record}
\usage{
onet_data_updates(path = NULL, force = FALSE)
}
\arguments{
\item{path}{Optional path to a local copy of the workbook. If supplied, no
download is attempted.}

\item{force}{Logical; re-download even when a cached copy exists.}
}
\value{
A tibble with \code{data_update_type}, \code{onet_soc_code}, \code{title}, and the
normalized columns published in the workbook, such as
\code{number_of_updates_as_of_08_2025}, \code{current_date}, \code{current_database}, and
previous update date/database columns.
}
\description{
Downloads and parses the official O*NET Longitudinal Data Updates workbook,
which records which occupations were re-rated in each data collection stream.
Use it as ground truth alongside the change classification from
\code{\link[=onet_panel_reconcile]{onet_panel_reconcile()}}: a \code{real_update} row for an occupation the official
record says was not re-rated deserves scrutiny, and vice versa.
}
\details{
The current workbook contains separate sheets for Incumbent or OE updates,
Abilities Analyst updates, and Skills Analyst updates. \code{onet_data_updates()}
combines those sheets and preserves the sheet label in \code{data_update_type}.

O*NET restates historical update counts against the current O*NET-SOC
2019 taxonomy, so per-cycle counts are not comparable across taxonomy
versions without bridging; see \code{\link[=onet_crosswalk_bridge]{onet_crosswalk_bridge()}}.

The file is cached under \code{tools::R_user_dir("onet2r", "cache")} in the
\code{reference} section; clear it with \code{onet_cache_clear(what = "reference")}.
}
\examples{
if (interactive()) {
  updates <- onet_data_updates()
  head(updates)
}
}
