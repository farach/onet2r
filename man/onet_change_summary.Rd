% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{onet_change_summary}
\alias{onet_change_summary}
\title{Summarise O*NET Panel Change Types}
\usage{
onet_change_summary(reconciled, by = c("overall", "job_family"))
}
\arguments{
\item{reconciled}{A tibble from \code{\link[=onet_panel_reconcile]{onet_panel_reconcile()}}.}

\item{by}{Summary level to return. \code{"overall"} returns one package-level row;
\code{"job_family"} returns the overall row plus one row per two-digit SOC
family.}
}
\value{
A tibble with one row per summary group and change type. The \code{n}
column counts rows of that change type within the group, and \code{share} is
the within-group share.
}
\description{
Counts reconciliation change types overall and by two-digit job family.
}
\examples{
reconciled <- tibble::tibble(
  to_soc_code = c("11-1011", "11-1011", "15-1252"),
  change_type = factor(c("real_update", "stale_carryforward", "real_update")),
  safely_comparable = c(TRUE, TRUE, TRUE)
)
onet_change_summary(reconciled)
}
