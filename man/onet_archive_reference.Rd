% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{onet_archive_reference}
\alias{onet_archive_reference}
\title{Read an O*NET Archive Reference Table}
\usage{
onet_archive_reference(version, table, path = NULL, release_date = NULL)
}
\arguments{
\item{version}{O*NET database version, for example \code{"30.3"}.}

\item{table}{Archive table name, for example \code{"GWAs to IWAs to DWAs"} or
\code{"Scales Reference"}.}

\item{path}{Optional path to a local text archive ZIP file or extracted
archive directory. If supplied, no download is attempted.}

\item{release_date}{Optional release date for a local archive. Downloaded
archives use the release date from \code{\link[=onet_releases]{onet_releases()}}.}
}
\value{
A tibble with \code{release_version}, \code{release_date}, and the table's
published columns renamed to snake case, with \code{O*NET} written as \code{onet}.
Columns whose names end in \verb{_id} or \verb{_code} stay character so they join
to \code{\link[=onet_archive_read]{onet_archive_read()}} output. Other columns are converted with
\code{\link[utils:type.convert]{utils::type.convert()}}.
}
\description{
Reads a table from an O*NET text archive with its published columns.
Use it for reference and lookup tables that have no O*NET-SOC code
column, such as \code{"GWAs to IWAs to DWAs"}, \code{"Scales Reference"},
\code{"Task Categories"}, and \code{"Content Model Reference"}, which
\code{\link[=onet_archive_read]{onet_archive_read()}} does not read. It also returns columns that the
longitudinal panel schema does not keep, such as \code{job_zone} in
\code{"Job Zones"}.
}
\details{
Recent tab-delimited text archives (30.2 through 31.0 were checked) keep
repeated names out of linking and rating files. \verb{Tasks to DWAs.txt} and
\verb{Task Ratings.txt} carry codes but not the names found in the Excel files, so
\code{\link[=onet_archive_read]{onet_archive_read()}} returns \code{NA} for fields such as \code{dwa_element_name},
\code{scale_name}, and \code{title}. Occupation titles and task text can be joined back
from \verb{Occupation Data} and \verb{Task Statements}, which \code{\link[=onet_archive_read]{onet_archive_read()}}
reads. Scale names are only in \verb{Scales Reference.txt}, and DWA titles only in
\verb{GWAs to IWAs to DWAs.txt} (release 30.3 onward) or \verb{DWA Reference.txt}
(earlier releases). Read those with \code{onet_archive_reference()}. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{dwa_titles <- onet_archive_reference("31.0", "GWAs to IWAs to DWAs", path = zip)
tasks_to_dwas <- onet_archive_read("31.0", "Tasks to DWAs", path = zip) |>
  dplyr::select(-"dwa_element_name") |>
  dplyr::left_join(
    dplyr::distinct(dwa_titles, dwa_element_id, dwa_element_name),
    by = "dwa_element_id",
    relationship = "many-to-one"
  )
}\if{html}{\out{</div>}}
}
\examples{
archive_dir <- system.file(
  "extdata",
  "onet-mini",
  "db_30_3_text",
  package = "onet2r"
)
onet_archive_reference(
  "30.3",
  "GWAs to IWAs to DWAs",
  path = archive_dir,
  release_date = "2026-05-01"
)
}
