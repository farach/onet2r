% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resurvey.R
\name{onet_condition_on_resurvey}
\alias{onet_condition_on_resurvey}
\title{Condition an O*NET Panel on Resurvey Events}
\usage{
onet_condition_on_resurvey(resurvey_panel, at_risk_only = FALSE)
}
\arguments{
\item{resurvey_panel}{A tibble from \code{\link[=onet_resurvey_panel]{onet_resurvey_panel()}}.}

\item{at_risk_only}{If \code{TRUE}, return only the resurveyed at-risk rows. The
default returns every row with its label so the excluded denominator stays
visible.}
}
\value{
The input tibble with two added columns: \code{selection_reason}, a factor
with levels \code{resurveyed}, \code{unrevisited}, \code{taxonomy_seam}, and \code{suppressed},
and \code{at_risk}, \code{TRUE} only when \code{selection_reason} is \code{resurveyed}.
}
\description{
Labels each row of an \code{\link[=onet_resurvey_panel]{onet_resurvey_panel()}} frame with the reason it is or
is not part of the at-risk set for change estimation. This is the resurvey
denominator: comparisons only carry information about task change when the
occupation was actually re-rated, so estimation should restrict to the rows
this function marks at risk.
}
\details{
Labels are assigned by precedence so structural exclusions win over the
resurvey signal:
\enumerate{
\item \code{taxonomy_seam} when the incoming transition crosses a seam (\code{seam_in}) or
the row is an Analyst - Transition carry-forward. The v25.1 SOC-2010 to
SOC-2018 carry-forward is never treated as a resurvey.
\item \code{suppressed} when \code{recommend_suppress} is \code{"Y"}.
\item \code{resurveyed} when the occupation survey clock advanced.
\item \code{unrevisited} otherwise, including a first appearance with no prior
release to compare against.
}
}
\examples{
panel <- tibble::tibble(
  release_version = rep(c("22.1", "23.1"), each = 2),
  release_date = rep(as.Date(c("2017-10-01", "2018-11-01")), each = 2),
  soc_vintage = "2010",
  onet_soc_code = rep(c("15-1132.00", "29-1141.00"), 2),
  soc_code = rep(c("15-1132", "29-1141"), 2),
  task_id = rep(c("1001", "1002"), 2),
  scale_id = "IM",
  data_value = c(4.1, 4.6, 4.1, 4.8),
  source_date = as.Date(c("2016-07-01", "2016-07-01", "2016-07-01", "2018-07-01")),
  domain_source = "Incumbent"
)
rp <- onet_resurvey_panel(panel)
onet_condition_on_resurvey(rp)
}
