% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{onet_oews_bridge}
\alias{onet_oews_bridge}
\title{Bridge O*NET Occupations to OEWS Reference Codes}
\usage{
onet_oews_bridge(occupations, weight_panel, occupation_code = "onet_soc_code")
}
\arguments{
\item{occupations}{O*NET-SOC codes to bridge: a character vector, a data
frame with an \code{occupation_code} column, or an occupation-level
\code{\link[=onet_measure]{onet_measure()}}. Usually the occupation scores you are aggregating.}

\item{weight_panel}{A weight panel from \code{\link[=onet_weight_panel_oews]{onet_weight_panel_oews()}} built from
May 2021 or later OEWS estimates, or any data frame with
\code{reference_soc_code} and \code{year} columns. National, state, metropolitan, and
industry panels all work.}

\item{occupation_code}{Occupation code column when \code{occupations} is a data
frame.}
}
\value{
A tibble with one row per O*NET-SOC code and columns
\code{from_onet_soc_code}, \code{from_soc_code}, \code{reference_soc_code}, \code{map_type},
\code{crosswalk_weight}, and \code{crosswalk_path}. \code{map_type} is \code{"direct"} when
the occupation's SOC is in the panel, \code{"oews_combination"} when OEWS
publishes the occupation inside a combined code that is in the panel, and
\code{"not_in_panel"} otherwise. Rows other than \code{"oews_combination"} keep the
occupation's own SOC as \code{reference_soc_code}, so aggregation treats them as
it would without a bridge. \code{\link[=onet_provenance]{onet_provenance()}} reports \code{crosswalk_path} for
aggregates that use the bridge.
}
\description{
Builds a bridge from O*NET-SOC codes to the reference SOC codes of an OEWS
weight panel, including the combined codes OEWS publishes in place of some
detailed SOC occupations. Pass the result to the \code{bridge} argument of
\code{\link[=onet_measure_aggregate]{onet_measure_aggregate()}} or \code{\link[=onet_measure_sensitivity]{onet_measure_sensitivity()}}.
}
\details{
Beginning with the May 2021 estimates, OEWS publishes most detailed 2018 SOC
occupations but combines some of them, either at the broad-occupation level
(for example \code{31-1120}, Home Health and Personal Care Aides, which holds
\code{31-1121} and \code{31-1122}) or as OEWS-specific codes (for example \code{25-9045},
Teaching Assistants, Except Postsecondary). Without a bridge, O*NET
occupations inside those codes cannot match the panel, and their employment
is left out of \code{covered_employment}.

The bridge uses the 12 combined codes, and the SOC occupations each one
includes, listed in the BLS May 2021 OEWS occupation definitions. The May
2023 through May 2025 national files publish the same 12 codes. Because the
list comes from those definitions rather than from the rows of
\code{weight_panel}, an occupation missing from a state, metropolitan, or industry
panel, for example because its estimate is suppressed, is never merged into a
neighboring combined code. It stays \code{"not_in_panel"}.

Occupations inside one combination are averaged with equal weight, the same
way \code{\link[=onet_measure_aggregate]{onet_measure_aggregate()}} averages several O*NET detail codes that
share one SOC. OEWS publishes no employment split among them.

May 2019 and May 2020 OEWS estimates use a hybrid of the 2010 and 2018 SOC
with different combined codes, and earlier estimates use older SOC versions,
so \code{weight_panel} must contain only years from 2021 on. Build a bridge by
hand for earlier panels.
}
\examples{
weights <- tibble::tibble(
  reference_soc_code = c("29-1141", "31-1120"),
  year = 2024L,
  employment = c(3000, 4000),
  weight_share = c(3, 4) / 7,
  source = "OEWS",
  source_taxonomy = "2018 SOC",
  reference_taxonomy = "2018 SOC"
)
# Stylized scores for illustration only.
scores <- tibble::tibble(
  onet_soc_code = c("29-1141.00", "31-1121.00", "31-1122.00"),
  measure_score = c(0.2, 0.4, 0.6)
)
bridge <- onet_oews_bridge(scores, weights)
bridge
onet_measure_aggregate(scores, weights, bridge = bridge, measure_id = "stylized")
}
