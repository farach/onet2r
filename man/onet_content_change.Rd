% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_change.R
\name{onet_content_change}
\alias{onet_content_change}
\title{Measure O*NET Content Change Between Releases}
\usage{
onet_content_change(
  panel,
  scale = "IM",
  item = "task_id",
  min_importance = NULL,
  from = NULL,
  to = NULL
)
}
\arguments{
\item{panel}{A tibble from \code{\link[=onet_panel]{onet_panel()}} (a Task Ratings panel) or the same
schema. Must contain \code{onet_soc_code}, \code{release_version}, \code{release_date},
\code{soc_vintage}, \code{data_value}, and the \code{item} key column. \code{scale_id} is
required when \code{scale} is not \code{NULL}.}

\item{scale}{Optional scale id used for the rating metrics and item
membership, for example \code{"IM"} for Importance. Use \code{NULL} to keep every
scale.}

\item{item}{Column that identifies the content item within an occupation.
Defaults to \code{"task_id"}. Item membership drives the set metrics.}

\item{min_importance}{Optional numeric floor applied to \code{data_value} on the
selected \code{scale}. Items below the floor, or with missing \code{data_value}, are
dropped before set membership is computed. Use it to apply the Importance
filter that removes the post-v21.0 Task Relevance artifact.}

\item{from, to}{Optional release versions selecting a single comparison. When
both are supplied only that pair is returned; otherwise every adjacent
release pair is compared in release-date order.}
}
\value{
A tibble with one row per occupation and release pair:
\code{n_from}, \code{n_to}, \code{n_added}, \code{n_dropped}, \code{n_retained}; \code{jaccard}
(set similarity, the retained share of the item union); \code{churn_rate}
(\code{1 - jaccard}); \code{rating_delta_l2} (the Euclidean norm of \code{data_value}
changes over retained items); \code{cosine} (cosine similarity of the
\code{data_value} vectors over the item union, missing items filled with zero);
and the seam flags \code{seam}, \code{seam_type}, and \code{safely_comparable}.
}
\description{
Computes task-set and rating content-change metrics for each occupation
between O*NET releases. This is the single source of content metrics for
longitudinal work, so robustness measures cannot silently diverge. It is
seam-aware: comparisons that cross the v21.0 Task Relevance retirement or the
v25.1 SOC-2010 to SOC-2018 change are flagged so taxonomy churn is not
counted as content churn.
}
\details{
Set metrics compare the \code{item} keys an occupation carries in each release.
\code{rating_delta_l2} uses only retained items, so it measures rating drift among
tasks that persist; \code{cosine} uses the union with zero fill, so it responds to
both membership turnover and rating change. Seam-crossing pairs still receive
metrics but are marked \code{safely_comparable = FALSE}; include them only in a
clearly labeled seam-inclusive row. Occupations are matched on
\code{onet_soc_code}, so cross-taxonomy pairs generally share few codes and should
be bridged with \code{\link[=onet_crosswalk_bridge]{onet_crosswalk_bridge()}} before interpretation.
}
\examples{
panel <- tibble::tibble(
  release_version = c(rep("22.1", 3), rep("23.1", 3)),
  release_date = c(rep(as.Date("2017-10-01"), 3), rep(as.Date("2018-11-01"), 3)),
  soc_vintage = "2010",
  onet_soc_code = "15-1132.00",
  task_id = c("1", "2", "3", "1", "2", "4"),
  scale_id = "IM",
  data_value = c(4.0, 3.5, 2.0, 4.5, 3.5, 3.0)
)
onet_content_change(panel)
}
